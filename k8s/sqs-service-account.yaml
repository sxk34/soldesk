apiVersion: v1
kind: ServiceAccount
metadata:
  name: sqs-access-sa
  namespace: ticketing
  annotations:
    # terraform output -raw sqs_access_role_arn 으로 확인 후 교체
    eks.amazonaws.com/role-arn: "arn:aws:iam::ACCOUNT_ID:role/ticketing-sqs-access-role"
